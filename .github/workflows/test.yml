name: Pipeline test 

on:
    push:
        branches:
            - dev

jobs:
  build:
        runs-on: [self-hotest, linux, x64]

        steps:
            - name: Checkout index
              run: test -f index.html